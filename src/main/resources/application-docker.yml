# Spring "docker" profile – overrides only what differs from application.yml
spring:
  datasource:
    url: jdbc:mysql://mysql:3306/furniture_shop

furnitureshop:
  avatar:
    directory: "/app/avatars/"

